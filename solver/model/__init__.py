"""Domain model for spec-kit-schedule.

This package holds the core domain dataclasses and small helpers shared by
the parser, scheduler, and replan modules. It deliberately contains no
solver-internal symbols.
"""
