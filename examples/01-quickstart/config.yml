# Zero-config quickstart: no agents, just two parallel subagent lanes.
# (Delete this file entirely to get the default of three lanes.)
workers: 2

solver:
  time_limit: 10
  num_workers: 1   # deterministic output for the frozen expected/out.json
